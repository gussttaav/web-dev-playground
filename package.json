{
    "dependencies": {
        "dotenv": "^16.0.3"
    },
    "scripts": {
        "build": "node js/config/generate-config.js",
        "start": "node js/config/generate-config.js && npx serve .",
        "vercel-build": "npm install && npm run build"
    }
}